'use strict';

describe('myApp.play module', function() {

  beforeEach(module('myApp.play'));

  describe('play controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('PlayCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
